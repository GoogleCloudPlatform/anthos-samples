apiVersion: gcp.upbound.io/v1beta1
kind: ProviderConfig
metadata:
  name: default-gcp
spec:
  projectID: anthos-multi-cloud-335819
  credentials:
    secretRef:
      key: creds
      name: gcp-creds
      namespace: crossplane-system
    source: Secret